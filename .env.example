# Gemini API key (text interpretation, image gen, TTS, STT)
GEMINI_API_KEY=

# Daily generation limit per IP (server-side enforcement)
DAILY_LIMIT_PER_IP=2
# Same value, exposed to the client for the "(used/limit)" badge
NEXT_PUBLIC_DAILY_LIMIT_PER_IP=2
